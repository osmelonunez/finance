[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast tests without HTTP requests
    routes: endpoint and authorization contract tests
    integration: tests backed by the isolated PostgreSQL database
